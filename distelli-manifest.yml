zchase/jekyll:

  PreInstall:
    - 'if [ -a /etc/nginx ]; then'
    -     echo "NGINX is already installed"
    - else
    -     sudo apt-get install nginx -y
    - fi
    - sudo rm -f /usr/share/nginx/html/*
    - mv -v _sites/* /usr/share/nginx/html/